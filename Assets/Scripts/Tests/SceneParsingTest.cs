using UnityEngine;

public class SceneParsingTest : MonoBehaviour
{
    // private SceneParsing _sp = new SceneParsing();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
